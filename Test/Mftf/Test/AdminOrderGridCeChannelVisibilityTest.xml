<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="AdminOrderGridCeChannelVisibilityTest">
        <annotations>
            <title value="Check if CE Channel grid column is displayed correctly"/>
            <stories value="Check order grid for CE Channel"/>
            <description value="CE Channel contains data exported from ChannelEngine."/>
            <severity value="MINOR"/>
            <group value="ChannelEngineExtension"/>
            <testCaseId value="CE-2" />
        </annotations>

        <actionGroup ref="AdminLoginActionGroup" stepKey="loginAsAdmin"/>

        <comment userInput="Opening orders page..." stepKey="openOrdersPageComment" />
        <actionGroup ref="AdminOrdersPageOpenActionGroup" stepKey="goToOrders"/>

        <comment userInput="Checking CE Channel in the order grid..." stepKey="seeCeChannelTextComment" />
        <seeElement stepKey="seeCeChannelText" selector="{{OrderGridSection.ceChannelSpan}}" />
    </test>
</tests>
